name: mcqc
channels:
  - pytorch
  - defaults
  - conda-forge
  - nvidia
dependencies:
  - python
  - pip
  - pytorch
  - torchvision
  - cudatoolkit
  - tensorboard
  - tqdm
  - scipy
  - matplotlib
  - pip:
    - pyyaml
    - cfmUtils
    - pyro-ppl
    - storchastic
    - lmdb
    # need gcc > 8, nvcc
    - compressai
    - packaging
